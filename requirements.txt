# ── Core data science ────────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.11
Pillow>=10.0
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3
tqdm>=4.66
PyYAML>=6.0

# ── Multi-label stratification ───────────────────────────────────────────────
scikit-multilearn>=0.2.0

# ── Deep learning ────────────────────────────────────────────────────────────
torch>=2.1
torchvision>=0.16
transformers>=4.40
accelerate>=0.27
peft>=0.10
bitsandbytes>=0.43          # 4-bit NF4 quantization for QLoRA

# ── Clinical NLP metrics ─────────────────────────────────────────────────────
f1chexbert>=0.0.2           # CheXbert labeler + F1-CheXbert metric
bert-score>=0.3.13          # BERTScore
evaluate>=0.4.1             # HuggingFace evaluate hub (BLEU, ROUGE)
rouge-score>=0.1.2

# ── MLOps ────────────────────────────────────────────────────────────────────
wandb>=0.17
dvc>=3.40

# ── Notebook / reporting ─────────────────────────────────────────────────────
ipykernel>=6.29
nbformat>=5.9

# ── Optional: RadGraph (may need separate install; see fallbacks in handout) ──
# radgraph>=1.0     # or: pip install rrg-metric
